version: "2"
sql:
  - engine: "sqlite"
    schema: "router/gtfsdb/sql/schema.sql"
    queries: "router/gtfsdb/sql/query.sql"
    gen:
      go:
        package: "db"
        out: "router/gtfsdb/db"
        emit_json_tags: true
        emit_empty_slices: true
